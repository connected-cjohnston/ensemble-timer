import React from "react";
import { Typography } from "@mui/material";

export default function Roles() {
  return (
    <>
      <Typography variant="h4">Roles</Typography>
      <Typography variant="p">Navigator</Typography>
      <Typography variant="p">Typist</Typography>
      <Typography variant="p">Co-Navigator</Typography>
      <Typography variant="p">Co-Navigator</Typography>
    </>
  );
}
